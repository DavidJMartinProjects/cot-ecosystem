apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cot-v3.fullname" . }}-test-connection"
  labels:
    {{- include "cot-v3.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cot-v3.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
