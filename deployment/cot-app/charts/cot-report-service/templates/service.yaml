apiVersion: v1
kind: Service
metadata:
  name: {{ include "cot-report-service.fullname" . }}
  labels:
    {{- include "cot-report-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cot-report-service.selectorLabels" . | nindent 4 }}
