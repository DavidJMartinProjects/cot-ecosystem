apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cot-report-service.fullname" . }}-test-connection"
  labels:
    {{- include "cot-report-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cot-report-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
