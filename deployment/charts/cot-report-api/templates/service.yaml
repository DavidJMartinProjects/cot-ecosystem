apiVersion: v1
kind: Service
metadata:
  name: {{ include "cot-report-api.fullname" . }}
  labels:
    {{- include "cot-report-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cot-report-api.selectorLabels" . | nindent 4 }}
