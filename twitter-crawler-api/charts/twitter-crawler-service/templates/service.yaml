apiVersion: v1
kind: Service
metadata:
  name: {{ include "twitter-crawler-service.fullname" . }}
  labels:
    {{- include "twitter-crawler-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "twitter-crawler-service.selectorLabels" . | nindent 4 }}
