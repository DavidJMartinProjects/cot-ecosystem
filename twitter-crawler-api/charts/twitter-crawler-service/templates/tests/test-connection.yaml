apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "twitter-crawler-service.fullname" . }}-test-connection"
  labels:
    {{- include "twitter-crawler-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "twitter-crawler-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
